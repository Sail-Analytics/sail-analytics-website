# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# This project uses bun (bun.lock) as its canonical lockfile; ignore npm's
# lockfile so a local `npm install` doesn't create a conflicting one.
package-lock.json

# Scratch debugging artifacts, not part of the app
supabase/diagnostics/
test-supabase-insert.mjs
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
